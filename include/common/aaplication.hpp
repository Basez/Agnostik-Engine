#pragma once

namespace AGN
{
	class AAplication
	{
	public:
		AAplication();
		~AAplication();
	
	private:
		AAplication(const AAplication& other);
		AAplication& operator=(const AAplication& other);

	};
}